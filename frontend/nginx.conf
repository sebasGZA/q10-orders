server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }

    location /orders {
        proxy_pass http://orders-api:8080/orders;
        proxy_set_header Host $host;
    }
}
